FILE(GLOB UTILS_SOURCES *.cpp)
FILE(GLOB UTILS_HEADERS *.h)

ADD_LIBRARY(utils_static STATIC ${UTILS_SOURCES} ${UTILS_HEADERS})
TARGET_INCLUDE_DIRECTORIES(utils_static PUBLIC ./ ../imgui-lib ${SDL2_INCLUDE_DIRS})
