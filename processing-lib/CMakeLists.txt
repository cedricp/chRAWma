FILE(GLOB PROCESSING_SOURCES *.cpp)
FILE(GLOB PROCESSING_HEADERS *.h)

ADD_LIBRARY(processing_static STATIC ${PROCESSING_SOURCES} ${PROCESSING_HEADERS})
TARGET_INCLUDE_DIRECTORIES(processing_static PUBLIC ./ ../utils ${LensFun_INCLUDE_DIRS})
